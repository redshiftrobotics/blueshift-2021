# Speedometer will likely need to be installed before this script will work
# Currently it only runs on the etheret port of the udoo bolt

# TODO: Add a way to specify the interface
speedometer -r enp4s0 -t enp4s0